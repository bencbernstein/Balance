# Balance React / Redux Front

####Start#####
Checkout this repo, install dependencies, then start the gulp process with the following:

```
	> git clone git@github.com:bencbernstein/Balance.git
    > cd Balance
	> npm install
	> npm start
```
